query ideasMain($token:String, $filter:String, $page:Int){
  ideas(token:$token, filterBy:$filter, startRank: $page){
    ... on IdeasField{
      author
      title
      body
      comments{
        author
        body
      }
      upvoter
    }
  }
}
